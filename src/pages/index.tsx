import type { NextPage } from "next";
import { Hero } from "../features/hero";

const Home: NextPage = () => {
    return (
        <>
            <Hero />
        </>
    );
};

export default Home;
